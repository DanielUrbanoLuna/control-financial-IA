import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  /* El compilador SWC de Next.js aplica el nuevo JSX transform automáticamente,
     no hace falta configurar nada adicional para evitar el import de React */
};

export default nextConfig;
